{
  "name": "chiwashira-ziwenga-family-tree",
  "version": "0.1.0",
  "private": true,
  "description": "Phase 0 — database schema and seed script for the Chiwashira–Ziwenga family tree",
  "scripts": {
    "db:migrate": "prisma migrate dev --name init",
    "db:seed": "prisma db seed",
    "db:studio": "prisma studio",
    "db:reset": "prisma migrate reset"
  },
  "prisma": {
    "seed": "tsx prisma/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.20.0"
  },
  "devDependencies": {
    "prisma": "^5.20.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "@types/node": "^22.7.0"
  }
}
